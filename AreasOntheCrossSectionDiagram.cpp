#include <iostream>
#include <cstdlib>
#include <string>
#include <vector>

using namespace std;

int main() {
    vector<char> inputs = {};

    char c;
    while (scanf("%c", &c) != EOF) {
        inputs.push_back(c);
    }
    
    for (size_t i = 0; i < inputs.size(); i++) {

    }
}
